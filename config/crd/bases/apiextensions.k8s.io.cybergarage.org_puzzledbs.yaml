---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: puzzledbs.apiextensions.k8s.io.cybergarage.org
spec:
  group: apiextensions.k8s.io.cybergarage.org
  names:
    kind: PuzzleDB
    listKind: PuzzleDBList
    plural: puzzledbs
    singular: puzzledb
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: PuzzleDB is the Schema for the puzzledbs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PuzzleDBSpec defines the desired state of PuzzleDB
            properties:
              foo:
                description: Foo is an example field of PuzzleDB. Edit puzzledb_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: PuzzleDBStatus defines the observed state of PuzzleDB
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
